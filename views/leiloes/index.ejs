<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="utf-8">
  <title>Leilões</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/css/leiloes.css">
  <script src="/js/main.js"></script>
</head>

<body>
  <header>
    <div>
      <p id="title">Leilões</p>
      <p id="subtitle">Encontre as melhores oportunidades e ofertas para você</p>
      <p id="subtitle">Aproveite agora mesmo !</p>
    </div>
  </header>

  <div>
    <img id="imagem" src="/assets/Leilao.jpg" alt="Leilões">
  </div>

  <form method="get" action="">
    <div id="select">
      <select name="city" id="cidade" onchange="this.form.submit()">
        <option value="" <%=!selectedCity ? 'selected' : '' %>>Todas as cidades</option>
        <% cidades.forEach(city=> { %>
          <option value="<%= city %>" <%=city===selectedCity ? 'selected' : '' %>>
            <%= city %>
          </option>
          <% }) %>
      </select>
    </div>
  </form>


  <div class="cards">
    <% eventos.forEach(evt=> { %>
      <div id="card">
        <p>Cidade: <%= evt.city %>
        </p>
        <p>Evento: <%= evt.title %>
        </p>
        <p>Data:
          <%= evt.date ? new Date(evt.date) .toLocaleDateString('pt-BR',{ day:'2-digit', month:'long' }) : '-' %>
        </p>
        <p>Local: <%= evt.location %>
        </p>
        <p>Descrição: <%= evt.description %>
        </p>
      </div>
      <% }) %>
  </div>

  <footer>
    <a onclick="window.location.href='/'"><button>Voltar</button></a>
  </footer>
</body>

</html>